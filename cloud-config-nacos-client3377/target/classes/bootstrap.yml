server:
  port: 3377

spring:
  application:
    name: cloud-config-nacos-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: DEFAULT_GROUP  #默认分组
        #group: TEST_GROUP  #自定义分组