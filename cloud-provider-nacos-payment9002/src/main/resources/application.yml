server:
  port: 9002

spring:
  application:
    name: cloud-provider-nacos-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

#暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: "*"